---
const steps = [
  {
    number: '01',
    title: 'Create your free account',
    description: 'Sign up in under a minute — no credit card, no trial period. Your account is free forever on the core plan.',
    icon: `<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path><circle cx="12" cy="7" r="4"></circle></svg>`,
  },
  {
    number: '02',
    title: 'Log your income, expenses & assets',
    description: 'Add your monthly income, log daily expenses by category, and build your asset portfolio. Takes just minutes to set up.',
    icon: `<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9"></path><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"></path></svg>`,
  },
  {
    number: '03',
    title: 'Get complete financial clarity',
    description: 'Your dashboard shows net worth, budget progress, debt payoff timeline, and spending trends — all updated in real time.',
    icon: `<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"></polyline></svg>`,
  },
];
---

<section id="how-it-works" class="py-16 lg:py-24 bg-slate-900/30">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Header -->
    <div class="text-center mb-10 reveal">
      <span class="inline-block text-emerald-400 text-sm font-semibold uppercase tracking-wider mb-4">How It Works</span>
      <h2 class="text-3xl sm:text-4xl lg:text-5xl font-extrabold text-white mb-5">
        Up and running in minutes
      </h2>
      <p class="text-slate-400 text-lg max-w-xl mx-auto">
        No lengthy setup, no tutorials required. Start seeing your financial picture immediately.
      </p>
    </div>

    <!-- Steps -->
    <div class="relative">
      <!-- Connector line (desktop) -->
      <div class="hidden lg:block absolute top-16 left-1/2 -translate-x-1/2 w-2/3 h-px bg-gradient-to-r from-transparent via-slate-700 to-transparent"></div>

      <div class="grid lg:grid-cols-3 gap-10 lg:gap-8">
        {steps.map((step, i) => (
          <div class={`reveal reveal-delay-${i + 1} text-center lg:text-left relative`}>
            <!-- Number + icon circle -->
            <div class="relative inline-flex items-center justify-center w-16 h-16 mb-6 mx-auto lg:mx-0">
              <div class="absolute inset-0 bg-emerald-500/20 rounded-2xl blur-md"></div>
              <div class="relative w-16 h-16 bg-slate-900 border border-emerald-500/30 rounded-2xl flex items-center justify-center text-emerald-400">
                <Fragment set:html={step.icon} />
              </div>
              <span class="absolute -top-2 -right-2 w-6 h-6 bg-emerald-500 text-white text-xs font-bold rounded-full flex items-center justify-center shadow-lg">
                {i + 1}
              </span>
            </div>

            <h3 class="text-white font-bold text-xl mb-3">{step.title}</h3>
            <p class="text-slate-400 text-sm leading-relaxed max-w-sm mx-auto lg:mx-0">
              {step.description}
            </p>
          </div>
        ))}
      </div>
    </div>

    <!-- Bottom highlight -->
    <div class="reveal mt-10 bg-gradient-to-r from-emerald-500/10 via-emerald-500/5 to-transparent border border-emerald-500/20 rounded-2xl p-5 sm:p-8 lg:p-10">
      <div class="flex flex-col lg:flex-row items-center gap-6">
        <div class="flex-shrink-0 w-14 h-14 bg-emerald-500/10 border border-emerald-500/20 rounded-xl flex items-center justify-center text-emerald-400">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
            <path d="M18 8h1a4 4 0 0 1 0 8h-1"></path>
            <path d="M2 8h16v9a4 4 0 0 1-4 4H6a4 4 0 0 1-4-4V8z"></path>
            <line x1="6" y1="1" x2="6" y2="4"></line>
            <line x1="10" y1="1" x2="10" y2="4"></line>
            <line x1="14" y1="1" x2="14" y2="4"></line>
          </svg>
        </div>
        <div class="text-center lg:text-left">
          <h4 class="text-white font-bold text-lg mb-1">Works offline too</h4>
          <p class="text-slate-400 text-sm">
            Install Expense Tracker as an app on your phone or desktop. Log expenses even without internet — everything syncs automatically when you're back online.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>
